% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arithmetic_annuity_av_tbl.R
\name{arithmetic_annuity_av_tbl}
\alias{arithmetic_annuity_av_tbl}
\title{Accumulated value of an arithmetic progression annuity}
\usage{
arithmetic_annuity_av_tbl(
  .data = NULL,
  amount = NULL,
  step = NULL,
  n = NULL,
  i = NULL,
  timing = c("immediate", "due", "vencida", "anticipada"),
  perpetuity = FALSE,
  col_amount = "amount",
  col_step = "step",
  col_n = "n",
  col_i = "i",
  .out = "av",
  .keep = c("all", "used", "none"),
  .na = c("propagate", "error", "drop")
)
}
\arguments{
\item{.data}{A data.frame or tibble. If \code{NULL}, inputs must be supplied
directly as scalars or equal-length vectors.}

\item{amount}{Numeric first payment when \code{.data = NULL}.}

\item{step}{Numeric arithmetic increment per payment period when \code{.data = NULL}.}

\item{n}{Number of payments when \code{.data = NULL}.}

\item{i}{Effective interest rate per payment period when \code{.data = NULL}.}

\item{timing}{Payment timing. Accepted values are \code{"immediate"}, \code{"due"},
\code{"vencida"}, and \code{"anticipada"}.}

\item{perpetuity}{Logical. Must be \code{FALSE}. Included only for interface
consistency with the present-value companion function.}

\item{col_amount}{Name of the first-payment column.}

\item{col_step}{Name of the arithmetic-step column.}

\item{col_n}{Name of the number-of-payments column.}

\item{col_i}{Name of the interest-rate column.}

\item{.out}{Name of the output column containing accumulated value.}

\item{.keep}{One of \code{"all"}, \code{"used"}, or \code{"none"}.}

\item{.na}{NA handling policy: \code{"propagate"}, \code{"error"}, or \code{"drop"}.}
}
\value{
A tibble with a new numeric column named by \code{.out}.
}
\description{
Computes the accumulated value of an annuity whose payments follow an
arithmetic progression.
}
\details{
The first payment is \code{amount}, and each subsequent payment changes by
\code{step}. The annuity may be either:
\itemize{
\item annuity-immediate (\code{timing = "immediate"} or \code{"vencida"}),
\item annuity-due (\code{timing = "due"} or \code{"anticipada"}).
}

This is a tibble-first mutate-style function: each input row is one case.

Assumptions:
\itemize{
\item Time is discrete.
\item \code{i} is the effective interest rate per payment period.
\item \code{n} is the number of payments.
\item All payments must remain nonnegative: the final payment
\code{amount + (n - 1) * step} must be >= 0.
\item \code{perpetuity = TRUE} is not supported because the accumulated value
of a perpetuity is not finite.
}

For an annuity-immediate with \code{n} payments, first payment \code{P}, step \code{Q},
and effective rate \code{i} per period, the accumulated value at time \code{n} is:
\deqn{AV = P \, s_{\overline{n|}} + Q \, \frac{s_{\overline{n|}} - n}{i}}{AV = P s_n| + Q s_n| - ni}
where \eqn{s_{\overline{n|}} = \frac{(1+i)^n - 1}{i}}.

For an annuity-due the result is \eqn{(1+i)} times the immediate value.

When \eqn{i = 0}, the accumulated value simplifies to:
\deqn{AV = n P + Q \frac{n(n-1)}{2}.}{AV = n P + Q (n(n-1))/(2).}
}
\examples{
arithmetic_annuity_av_tbl(
  amount = 100,
  step = 5,
  n = 5,
  i = 0.05,
  timing = "immediate"
)

cases <- tibble::tibble(
  amount = c(100, 200, 50),
  step   = c(5, 10, 0),
  n      = c(5, 4, 10),
  i      = c(0.05, 0.04, 0.03)
)

arithmetic_annuity_av_tbl(
  cases,
  timing = "due",
  .out = "av_due"
)

}
\references{
Marcel B. Finan, \emph{A Basic Course in the Theory of Interest and
Derivatives Markets: A Preparation for the Actuarial Exam FM/2}.

Kellison, S. G. \emph{The Theory of Interest}.
}
\seealso{
\code{\link{arithmetic_annuity_pv_tbl}}, \code{\link{a_angle}}, \code{\link{s_angle}}

Other annuities: 
\code{\link{Da_angle}()},
\code{\link{Da_angle_tbl}()},
\code{\link{Ia_angle}()},
\code{\link{Ia_angle_tbl}()},
\code{\link{a_angle}()},
\code{\link{a_angle_tbl}()},
\code{\link{arithmetic_annuity_pv_tbl}()},
\code{\link{ga_angle}()},
\code{\link{ga_angle_tbl}()},
\code{\link{gs_angle}()},
\code{\link{gs_angle_tbl}()},
\code{\link{s_angle}()},
\code{\link{s_angle_tbl}()}
}
\concept{annuities}
