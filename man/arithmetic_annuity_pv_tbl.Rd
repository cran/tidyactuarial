% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arithmetic_annuity_pv_tbl.R
\name{arithmetic_annuity_pv_tbl}
\alias{arithmetic_annuity_pv_tbl}
\title{Present value of an arithmetic progression annuity}
\usage{
arithmetic_annuity_pv_tbl(
  .data = NULL,
  amount = NULL,
  step = NULL,
  n = NULL,
  i = NULL,
  timing = c("immediate", "due", "vencida", "anticipada"),
  perpetuity = FALSE,
  col_amount = "amount",
  col_step = "step",
  col_n = "n",
  col_i = "i",
  .out = "pv",
  .keep = c("all", "used", "none"),
  .na = c("propagate", "error", "drop")
)
}
\arguments{
\item{.data}{A data.frame or tibble. If \code{NULL}, inputs must be supplied
directly as scalars or equal-length vectors.}

\item{amount}{Numeric first payment when \code{.data = NULL}.}

\item{step}{Numeric arithmetic increment per payment period when \code{.data = NULL}.}

\item{n}{Number of payments when \code{.data = NULL} and \code{perpetuity = FALSE}.}

\item{i}{Effective interest rate per payment period when \code{.data = NULL}.}

\item{timing}{Payment timing. Accepted values are \code{"immediate"}, \code{"due"},
\code{"vencida"}, and \code{"anticipada"}.}

\item{perpetuity}{Logical; if \code{TRUE}, computes a perpetuity instead of a
finite annuity.}

\item{col_amount}{Name of the first-payment column.}

\item{col_step}{Name of the arithmetic-step column.}

\item{col_n}{Name of the number-of-payments column.}

\item{col_i}{Name of the interest-rate column.}

\item{.out}{Name of the output column containing present value.}

\item{.keep}{One of \code{"all"}, \code{"used"}, or \code{"none"}.}

\item{.na}{NA handling policy: \code{"propagate"}, \code{"error"}, or \code{"drop"}.}
}
\value{
A tibble with a new numeric column named by \code{.out}.
}
\description{
Computes the present value of an annuity whose payments follow an
arithmetic progression.
}
\details{
The first payment is \code{amount}, and each subsequent payment changes by
\code{step}. The annuity may be either:
\itemize{
\item annuity-immediate (\code{timing = "immediate"} or \code{"vencida"}),
\item annuity-due (\code{timing = "due"} or \code{"anticipada"}),
\item finite (\code{perpetuity = FALSE}),
\item or perpetual (\code{perpetuity = TRUE}).
}

This is a tibble-first mutate-style function: each input row is one case.

Assumptions:
\itemize{
\item Time is discrete.
\item \code{i} is the effective interest rate per payment period.
\item If \code{perpetuity = FALSE}, \code{n} is the number of payments.
\item If \code{perpetuity = TRUE}, \code{n} is ignored.
\item All payments must remain nonnegative: for finite annuities the
final payment \code{amount + (n - 1) * step} must be >= 0; for
perpetuities, \code{step >= 0} is required.
}

For a finite annuity-immediate with \code{n} payments, first payment \code{P},
arithmetic step \code{Q}, and effective rate \code{i} per period:
\deqn{PV = P \, a_{\overline{n|}} + Q \, \frac{a_{\overline{n|}} - n v^n}{i}}{PV = P a_n| + Q a_n| - n v^ni}
where \eqn{a_{\overline{n|}} = (1 - v^n)/i} and \eqn{v = 1/(1+i)}.

For an annuity-due, replace \eqn{a_{\overline{n|}}} by
\eqn{\ddot{a}_{\overline{n|}} = (1+i) a_{\overline{n|}}} and divide the step
component by \eqn{d = i/(1+i)} instead of \eqn{i}.

For a perpetuity-immediate (\eqn{i > 0}):
\deqn{PV = \frac{P}{i} + \frac{Q}{i^2}.}{PV = (P)/(i) + (Q)/(i^2).}

For a perpetuity-due:
\deqn{PV = \frac{P}{d} + \frac{Q}{i \, d} = (1+i)\left(\frac{P}{i} + \frac{Q}{i^2}\right).}{PV = (P)/(d) + (Q)/(i d) = (1+i)((P)/(i) + (Q)/(i^2)).}

When \eqn{i = 0} (finite case only), the present value simplifies to:
\deqn{PV = n P + Q \frac{n(n-1)}{2}.}{PV = n P + Q (n(n-1))/(2).}
}
\examples{
# Simple example: finite annuity-immediate
arithmetic_annuity_pv_tbl(
  amount = 100,
  step = 5,
  n = 5,
  i = 0.05,
  timing = "immediate"
)

# Medium example: finite annuity-due for multiple rows
cases <- tibble::tibble(
  amount = c(100, 200, 50),
  step   = c(5, 10, 0),
  n      = c(5, 4, 10),
  i      = c(0.05, 0.04, 0.03)
)

arithmetic_annuity_pv_tbl(
  cases,
  timing = "due",
  perpetuity = FALSE,
  .out = "pv_due"
)

# Perpetuity-due
arithmetic_annuity_pv_tbl(
  amount = 1000,
  step = 40,
  i = 0.10,
  timing = "due",
  perpetuity = TRUE
)

}
\references{
Marcel B. Finan, \emph{A Basic Course in the Theory of Interest and
Derivatives Markets: A Preparation for the Actuarial Exam FM/2}.

Kellison, S. G. \emph{The Theory of Interest}.
}
\seealso{
\code{\link{arithmetic_annuity_av_tbl}}, \code{\link{a_angle}}, \code{\link{s_angle}}

Other annuities: 
\code{\link{Da_angle}()},
\code{\link{Da_angle_tbl}()},
\code{\link{Ia_angle}()},
\code{\link{Ia_angle_tbl}()},
\code{\link{a_angle}()},
\code{\link{a_angle_tbl}()},
\code{\link{arithmetic_annuity_av_tbl}()},
\code{\link{ga_angle}()},
\code{\link{ga_angle_tbl}()},
\code{\link{gs_angle}()},
\code{\link{gs_angle_tbl}()},
\code{\link{s_angle}()},
\code{\link{s_angle_tbl}()}
}
\concept{annuities}
